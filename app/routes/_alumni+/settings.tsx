// Use url params to show tabs
// this will have a tab view.
// if in future there is more logic than using a outlet along with a sidebar,
// should be preferred instead of cramping everthing a single component

export default function SettingsPage() {
  return <div>WIP : Alumni Settings Page</div>;
}
